Prompt:

Generate a complete project template for *Parallelizing Blockchain Computations via Sharding (using PBFT and MPI)*.

Use terminal commands only to:

1. Create all necessary directories and files for a C++ + MPI project (`src/`, `include/`, `tests/`, `results/`, `build/`, etc.).
2. Create an empty Makefile placeholder for future build commands using `mpic++`.
3. Generate a STRUCTURE.md file that describes every directory and file, explaining its purpose and connection to the overall project.

⚠️ Do not generate any source code or implementation content — only structure, file names, and documentation placeholders.

Use @PROPOSAL.md and all files under @resources/ for contextual guidance when determining which directories and files are required.
